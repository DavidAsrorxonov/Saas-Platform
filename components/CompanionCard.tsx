import React from "react";

const CompanionCard = ({
  id,
  name,
  topic,
  subject,
  duration,
  color,
}: CompanionCardProps) => {
  return <div>CompanionCard</div>;
};

export default CompanionCard;
